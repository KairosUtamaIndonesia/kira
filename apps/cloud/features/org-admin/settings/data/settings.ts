// Org-admin settings data.
// Mutations (rename, delete org) are Platform Console-only until org self-service is built.
// Add org-scoped settings reads here as the Organization Admin surface grows.
const ORG_SETTINGS_VERSION = 1;

export { ORG_SETTINGS_VERSION };
