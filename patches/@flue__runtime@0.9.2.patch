diff -ruN a/dist/providers-DJxCeuC_.d.mts b/dist/providers-DJxCeuC_.d.mts
--- a/dist/providers-DJxCeuC_.d.mts
+++ b/dist/providers-DJxCeuC_.d.mts
@@ -78,8 +78,10 @@
    * Overridden per-model via {@link models}. Unset is `0`.
    */
   maxTokens?: number;
-  /** Per-model overrides for {@link contextWindow} and {@link maxTokens}, keyed by model ID. */
+  /** Per-model overrides keyed by the public model ID used in model specifiers. */
   models?: Record<string, {
+    /** Provider-facing model ID sent on the wire. Defaults to the public model ID. */
+    upstreamModelId?: string;
     contextWindow?: number;
     maxTokens?: number;
   }>;
diff -ruN a/dist/sandbox-C2jFycj2.mjs b/dist/sandbox-C2jFycj2.mjs
--- a/dist/sandbox-C2jFycj2.mjs
+++ b/dist/sandbox-C2jFycj2.mjs
@@ -800,8 +800,10 @@
 			maxTokens: 0
 		}, registration.binding, registration.gateway);
 	}
+	const modelRegistration = registration.models?.[modelId];
+	const upstreamModelId = modelRegistration?.upstreamModelId ?? modelId;
 	return {
-		id: modelId,
+		id: upstreamModelId,
 		name: modelId,
 		api: registration.api,
 		provider: providerId,
@@ -814,8 +816,8 @@
 			cacheRead: 0,
 			cacheWrite: 0
 		},
-		contextWindow: registration.models?.[modelId]?.contextWindow ?? registration.contextWindow ?? 0,
-		maxTokens: registration.models?.[modelId]?.maxTokens ?? registration.maxTokens ?? 0,
+		contextWindow: modelRegistration?.contextWindow ?? registration.contextWindow ?? 0,
+		maxTokens: modelRegistration?.maxTokens ?? registration.maxTokens ?? 0,
 		headers: registration.headers
 	};
 }
